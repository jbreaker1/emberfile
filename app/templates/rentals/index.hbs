{{#list-filter
  filter=(action "filterByCity")
as |filteredResults|
}}
  <ul class="results">
    {{#each filteredResults as |rentalUnit|}}
      <li>{{rental-listing rental=rentalUnit destroyRental="destroyRental"}}</li>
    {{/each}}
  </ul>
{{/list-filter}}
